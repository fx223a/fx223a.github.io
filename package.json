{
  "name": "engine-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "find public -name .DS_Store -delete",
    "build": "astro build",
    "preview": "astro preview",
    "check:placeholders": "node scripts/check-placeholders.mjs",
    "deploy:check": "npm run check:placeholders && npm run build",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.9"
  }
}